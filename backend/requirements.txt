# =============================================================================
# PlumA — dependencias Python fijadas
# -----------------------------------------------------------------------------
# Generado con Python 3.12. Versiones fijadas para que builds reproducibles:
# la imagen que se construye hoy será idéntica a la que se construye dentro
# de seis meses.
#
# Actualización recomendada: ejecutar `pip list --outdated` y `pip-audit`
# antes de cada release, subir versiones, volver a probar.
# =============================================================================

# Web / API
fastapi==0.136.0
uvicorn[standard]==0.45.0
starlette==1.0.1
python-multipart==0.0.27
httpx==0.28.1
httpcore==1.0.9
h11==0.16.0
anyio==4.13.0

# Configuración y datos
pyyaml==6.0.3

# Procesamiento de documentos
pypdf==6.10.2
pypdfium2==5.7.1
python-docx==1.2.0
Pillow==12.2.0

# Dependencias transitivas fijadas (para reproducibilidad total)
pydantic==2.13.3
pydantic-core==2.46.3
typing-extensions==4.15.0
typing-inspection==0.4.2
annotated-doc==0.0.4
annotated-types==0.7.0
click==8.3.3
httptools==0.7.1
python-dotenv==1.2.2
uvloop==0.22.1 ; sys_platform != "win32"
watchfiles==1.1.1
websockets==16.0
certifi==2026.4.22
idna==3.15
lxml==6.1.0
